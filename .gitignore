# deps
node_modules/

# node 런타임 사이드카 바이너리(~100MB) — CI/로컬에서 desktop/scripts/fetch-node.mjs로 받음
desktop/src-tauri/binaries/

# secrets — NEVER commit credentials
.env
.env.*
!.env.example

# playwright auth / session state (contains login cookies)
.auth/
storageState.json

# run artifacts (may contain personal data)
snapshots/
screenshots/
.history/
*.png
*.jpg
traces/
*.zip
!desktop/src-tauri/icons/*.png

# temp downloads
.tmp/

# misc
.DS_Store
*.log
